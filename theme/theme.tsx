export const theme = {
  primary: {
    green: (opacity: number) => {
      return `rgba(10,200,5,${opacity} )`;
    },
    deepGreen: (opacity: number) => {
      return `rgba(10,175,5,${opacity} )`;
    },
    zinc: (opacity: number) => {
      return `rgba(30,30,30,${opacity} )`;
    },
  },
};
